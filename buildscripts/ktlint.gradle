apply plugin: "org.jlleitschuh.gradle.ktlint"

ktlint {
    version = "0.43.2"

    reporters {
        reporter "plain"
        reporter "checkstyle"
        reporter "html"
    }

    outputColorName = "RED"
}